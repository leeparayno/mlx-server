apiVersion: v1
kind: Secret
metadata:
  name: mlx-server-secrets
type: Opaque
stringData:
  # API Keys (base64 encode in production)
  DEFAULT_API_KEY: "sk-test-12345"

  # Add more secrets as needed
  # HUGGINGFACE_TOKEN: ""  # For private model downloads
